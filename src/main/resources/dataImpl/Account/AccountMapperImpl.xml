<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.data.Account.AccountMapper">

    <insert id="register" parameterType="com.example.demo.po.AccountPo">
        insert into nkg.account(name, pwd, email, authority) values (#{name},#{pwd},#{email},#{authority})
    </insert>

    <select id="selectPwdByName" resultType="com.example.demo.po.AccountPo">
        select * from nkg.account where name = #{name} limit 0,1
    </select>

</mapper>